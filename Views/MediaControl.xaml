<UserControl  x:Class="Kova.Views.MediaControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Kova.Views"
      xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
      xmlns:cmd="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Platform"
      xmlns:svl ="clr-namespace:WPFSoundVisualizationLib;assembly=WPFSoundVisualizationLib"
      xmlns:materialDesign="MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Slider.xaml"
      mc:Ignorable="d" d:DesignWidth="783" Height="90.116">
    <UserControl.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="AccentBackground"
                     Color="{DynamicResource AccentColor}" />
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Dark.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="{DynamicResource AccentBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="61*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="578*"/>
            <ColumnDefinition Width="44*"/>
            <ColumnDefinition Width="44*"/>
            <ColumnDefinition Width="61*"/>
            <ColumnDefinition Width="56*"/>
        </Grid.ColumnDefinitions>
        <Button BorderBrush="White" Style="{DynamicResource MetroCircleButtonStyle}"
                Margin="5,4,40,25" Grid.Column="2" Grid.RowSpan="2" Grid.ColumnSpan="2">
            <Rectangle  Width="21" Margin="4,0,0,0"
                        Height="23"
                        Fill="White" >

                <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_control_play}" />
                </Rectangle.OpacityMask>
            </Rectangle>
        </Button>

        <Button BorderBrush="White" Style="{DynamicResource MetroCircleButtonStyle}" Margin="21,18,48,25" 
                Grid.Column="3" Grid.RowSpan="2" Grid.ColumnSpan="2">
            <Rectangle  Width="18" Margin="2,0,0,0"
                        Height="14"
                        Fill="White" >
                <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_control_fastforward_variant}" />
                </Rectangle.OpacityMask>
            </Rectangle>
        </Button>

        <Button BorderBrush="White" Style="{DynamicResource MetroCircleButtonStyle}" Margin="0.667,18,39,25" Grid.RowSpan="2" Grid.ColumnSpan="2" Grid.Column="1">
            <Rectangle Width="18" Margin="0,0,0,0"
                       Height="14"
                       Fill="White" >
                <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_control_rewind_variant}" />
                </Rectangle.OpacityMask>
            </Rectangle>
        </Button>

        <Slider Minimum="0" Maximum="100" 
                    Value="0" Foreground="White" HorizontalAlignment="Center"
                    Margin="18,45,47,0" Height="20" VerticalAlignment="Top" Width="718" Grid.ColumnSpan="5" Grid.Row="1" >
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="ValueChanged">
                    <cmd:EventToCommand Command="{Binding SeekValueChangedCommand, Mode=OneWay}"
                                                MustToggleIsEnabledValue="True" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Slider>

        <svl:AlbumArtDisplay Margin="18,17,508.333,28" Grid.RowSpan="2"/>
        <TextBlock x:Name="CurrentTrack" FontSize="17"
                   Foreground="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                   HorizontalAlignment="Left"
                   Margin="79,0,0,0" 
                   TextWrapping="Wrap" 
                   Text="{Binding CurrentSong.Title}" 
                   VerticalAlignment="Top" Width="266" Height="23" Grid.Row="1"/>
        <TextBlock x:Name="textBlock" FontSize="14"
                     Foreground="{DynamicResource ControlBorderBrush}"
                   Text="{Binding CurrentSong.Artist}" 
                   HorizontalAlignment="Left" 
                   Margin="79,21,0,0" 
                   Grid.Row="1" 
                   TextWrapping="Wrap" 
                   VerticalAlignment="Top" 
                   Height="19" Width="124"/>

        <ToggleButton BorderBrush="White" Style="{DynamicResource MetroCircleToggleButtonStyle}" Grid.Column="4" Margin="12,5,8,49" Grid.RowSpan="2">
            <Rectangle Width="15"
                               Height="15"
                               Fill="White">
                <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_repeat}" />
                </Rectangle.OpacityMask>
            </Rectangle>
        </ToggleButton>

        <ToggleButton BorderBrush="White" Style="{DynamicResource MetroCircleToggleButtonStyle}" Grid.Column="4" Margin="12,13,8,19" Grid.Row="1">
            <Rectangle Width="15"
                               Height="15"
                               Fill="White">
                <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_axis_three}" />
                </Rectangle.OpacityMask>
            </Rectangle>
        </ToggleButton>
    </Grid>
</UserControl>
